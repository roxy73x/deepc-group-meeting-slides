这里的“投影系数”就是把**当前想要跟踪的参考轨迹**，放到 DeePC 的历史数据字典里，看看它大概需要哪些 Hankel 列组合出来。

DeePC 里本来就有这个关系：

[
\begin{bmatrix}
U_p \
Y_p \
U_f \
Y_f
\end{bmatrix} g
===============

\begin{bmatrix}
u_{\mathrm{ini}} \
y_{\mathrm{ini}} \
u \
y
\end{bmatrix}
]

其中左边这个大矩阵可以记成：

[
H =
\begin{bmatrix}
U_p \
Y_p \
U_f \
Y_f
\end{bmatrix}
]

每一列就是历史数据里截取出来的一段输入输出轨迹。
(g) 是组合系数，表示“用哪些历史轨迹列组合出当前预测轨迹”。

---

在数据选择时，我们先构造一个“参考轨迹对应的目标向量”：

[
z_{\mathrm{ref}} =
\begin{bmatrix}
u_{\mathrm{ini}} \
y_{\mathrm{ini}} \
u_{\mathrm{ref}} \
y_{\mathrm{ref}}
\end{bmatrix}
]

然后求：

[
g_r = H^\dagger z_{\mathrm{ref}}
]

更实际一点，就是解一个最小二乘问题：

[
g_r =
\arg\min_g
\left|
Hg - z_{\mathrm{ref}}
\right|_2^2
]

如果担心噪声或病态，可以写成带正则的形式：

[
g_r =
\arg\min_g
\left|
Hg - z_{\mathrm{ref}}
\right|_2^2
+
\epsilon |g|_2^2
]

所以 (g_r) 的含义是：

> 当前参考轨迹如果要由历史 Hankel 数据列组合出来，大概需要每一列贡献多少。

如果某一项 (|(g_r)_i|) 很大，说明第 (i) 个 Hankel 列在重构当前参考时贡献更大，于是认为它是“更相关的数据列”。

然后选：

[
I_k = \operatorname{TopK}_i\left(|(g_r)_i|\right)
]

也就是取 (|g_r|) 最大的若干列作为当前相关列。

---

汇报里可以这样讲：

> 这里的投影系数不是额外训练出来的，而是利用 DeePC 本身的数据字典得到的。具体来说，我把当前历史输入输出和未来参考拼成一个目标向量，然后用 Hankel 数据矩阵做一次最小二乘投影，得到 (g_r)。(|(g_r)_i|) 越大，说明第 (i) 个历史数据片段对当前参考轨迹的重构贡献越大，因此优先降低这些列的正则化惩罚。

一句话版：

> (g_r) 是当前参考轨迹在 Hankel 数据字典上的最小二乘投影系数，用 (|g_r|) 排序即可得到当前更相关的数据列。
